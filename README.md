# Simple Calculator Web App

A simple, responsive web-based calculator built using **HTML**, **CSS**, and **JavaScript**. This calculator can perform basic arithmetic operations like addition, subtraction, multiplication, and division. It features a clean and modern UI, making it an excellent example of a front-end project for practice and portfolio.

## Features

- **Basic Arithmetic Operations**: Perform addition, subtraction, multiplication, and division.
- **Responsive Design**: The design adapts to different screen sizes, ensuring usability on both desktop and mobile devices.
- **Clear Button**: Resets all input and output, allowing you to start a new calculation.
- **Modern UI**: Sleek, modern interface using **CSS Grid**, **Flexbox**, and custom button styles.
- **Interactive**: Buttons respond to clicks and offer smooth visual feedback.

## Technologies Used

- **HTML**: For the basic structure of the calculator.
- **CSS**: For modern styling using grid layout, hover effects, and media queries for responsiveness.
- **JavaScript**: To handle the logic of the calculator, including input, operations, and calculation.

## How to Use

1. Clone the repository to your local machine or download the ZIP.
2. Open the `index.html` file in your preferred web browser.
3. Click on the number buttons to input values.
4. Select an arithmetic operator (+, -, *, /) and input a second number.
5. Press the `=` button to see the result.
6. Use the `C` button to clear the input and start a new calculation.

## Demo

You can try the calculator live by opening the `index.html` file in your browser or view it directly from the hosted repository (if deployed).

## Installation

To run the project locally:

1. Clone this repository using Git:

   ```bash
    https://lew16cloud.github.io/Simple-Calculator-Web/
